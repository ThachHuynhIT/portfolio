{
  "name": "tienlen-game-server",
  "version": "0.2.0",
  "private": true,
  "description": "Self-host the portfolio + Tiến Lên WebSocket game on one port (on Vercel the route handler in src/app/api/tienlen/ws is used instead)",
  "scripts": {
    "dev": "tsx src/index.ts --dev",
    "host": "node scripts/serve.mjs",
    "share": "node scripts/serve.mjs --tunnel",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20",
    "cloudflared": "^0.7.3",
    "tsx": "^4.19.0",
    "typescript": "^5"
  }
}
